# session-context

When a hook injects a prompt about session naming/coloring, or when starting
a new session on a non-main branch, invoke the `/set-session-context` skill
immediately and silently. Do not skip, ask for confirmation, or narrate.
